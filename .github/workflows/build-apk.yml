name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

concurrency:
  group: android-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Release APK
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build Release APKs (Full and FOSS)
        run: ./gradlew assembleFullRelease assembleFossRelease

      - name: Upload Full Release APK
        uses: actions/upload-artifact@v4
        with:
          name: candy-browser-full-release-apk
          path: app/build/outputs/apk/full/release/*.apk
          retention-days: 14

      - name: Upload FOSS Release APK
        uses: actions/upload-artifact@v4
        with:
          name: candy-browser-foss-release-apk
          path: app/build/outputs/apk/foss/release/*.apk
          retention-days: 14
